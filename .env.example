# Archetype Controller Configuration Example
# Copy this file to .env and customize as needed:
#   cp .env.example .env
#
# Generate secure secrets with:
#   openssl rand -hex 32

# ===================
# Web UI Settings
# ===================
WEB_PORT=8080

# ===================
# API Settings
# ===================
API_PORT=8000

# Database (uses embedded postgres by default)
# DATABASE_URL=postgresql+psycopg://netlab:netlab@postgres:5432/netlab_gui

# Redis (uses embedded redis by default)
# REDIS_URL=redis://redis:6379/0

# ===================
# Security (REQUIRED)
# ===================
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
SESSION_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# ===================
# Admin Account
# ===================
# Optional: Create an admin user on first startup
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme123

# ===================
# Authentication
# ===================
LOCAL_AUTH_ENABLED=true

# OIDC/SSO (optional)
# OIDC_ISSUER_URL=https://accounts.google.com
# OIDC_CLIENT_ID=your-client-id
# OIDC_CLIENT_SECRET=your-client-secret
# OIDC_REDIRECT_URI=http://localhost:8000/auth/oidc/callback
# OIDC_SCOPES=openid profile email
# OIDC_APP_REDIRECT_URL=http://localhost:8080/auth/callback

# ===================
# Limits
# ===================
MAX_CONCURRENT_JOBS_PER_USER=2

# ===================
# Local Agent (optional)
# ===================
# Set these if running the built-in agent container
# ARCHETYPE_AGENT_NAME=local-agent
# ARCHETYPE_AGENT_LOCAL_IP=192.168.1.100
